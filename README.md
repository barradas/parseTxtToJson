# Parse Txt to Json

## What does this cli tool do:
This cli tool converts a text file to a JSON array. Each line in the txt file will become an element in the array.

## How to use:
In the terminal run
```
parseTxtToJson parse --filepath /Users/myUser/documents/mytext.txt --output /Users/myUser/documents/myJson.json
```
